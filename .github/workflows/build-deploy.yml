name: build and deploy code

on: [push, pull_request]

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: pullimg git repo
              uses: actions/checkout@v2
            - name: say hi to kiran
              run: echo "hello beast"